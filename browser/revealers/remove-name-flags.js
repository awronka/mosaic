var flag = document.getElementsByClassName('MosaicDOMRevealerHoverStateFlag');

if (flag) {
	// If a flag is found when the extension is initiated, remove all flags
	$('.MosaicDOMRevealerHoverStateFlag').remove()
}